use soroban_shield_contracts::contracts::rate_limiter;
use soroban_sdk::{Address, Env};

pub fn throttle(env: &Env, caller: &Address, now: u64) {
    let _ = rate_limiter::check_and_record(env, caller, now);
}
