use soroban_shield_contracts::contracts::multi_sig;
use soroban_sdk::{Address, Env, Symbol};

pub fn propose(env: &Env, creator: &Address, action: Symbol, expiry: u32) -> u64 {
    multi_sig::create_proposal(env, creator, action, expiry)
}
